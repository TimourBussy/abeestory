services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: abeestory-app
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
